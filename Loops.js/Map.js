// Map function = Used to do manipulaton over the data.

const arr = [11, 22, 33, 44, 55, 66, 77, 88, 99];
const doubleNumbers = arr.map((items) => {
  return items * 2;
});
console.log(doubleNumbers);
