const arr = [11, 22, 33, 44, 55, 66, 77, 88, 99, 109, 119, 129];

// console.log(arr.length);// Gives length of the array.

// const push = arr.push(88); // adds element at endng of the array.
// console.log(arr);

// const pop = arr.pop(); // Deletes element from ending of the array.
// console.log(arr);

// const unshift = arr.unshift(5); //adds new element at begining of the array.
// console.log(arr);

// const shift = arr.shift(); // Deletes element from the beging of the array.
// console.log(arr);

// const include = arr.includes(200); // checks that element is present in the array or not.
// console.log(include);

// const index = arr.IndexOf(44); // Checks the index of the particular element.
// console.log(index);

// const slice = arr.slice(1, 4); // neglects last index. it does not changes the oroginal array.
// console.log(slice);

// const splice = arr.splice(1, 4); // Does not neglects last index. it chages the original array.
// console.log(splice);

// const del = arr.splice(4, 1); // Method to delete a single element from the array.
// console.log(arr);
